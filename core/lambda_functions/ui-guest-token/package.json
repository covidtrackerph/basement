{
  "name": "ui-guest-token",
  "version": "1.0.0",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc && cp package.json yarn.lock build && cd build && yarn --production"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jsdom": "^16.2.3",
    "@types/node": "^13.7.0",
    "@types/urijs": "^1.19.8",
    "typescript": "^3.7.5"
  }
}
